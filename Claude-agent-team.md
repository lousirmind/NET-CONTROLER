# Claude Agent Team — 多 Agent 协作开发工作流

> 用于代码优化、Bug 修复、功能升级等需要多角色协作的软件开发任务。
> 可复用的角色定义、Prompt 模板与协作流程。

---

## 一、适用场景

- 遗留项目接手后的 Bug 修复与重构
- 有一定复杂度的功能升级（跨多个文件/模块）
- 需要同时考虑决策、实施、验证三个维度的任务

**不适用场景**：
- 单文件小改动（直接对话即可）
- 纯调研/分析任务（单个 Explore Agent 足够）

---

## 二、角色体系

### 2.1 角色一：决策者（Decision Maker）

**职责**：分析现状 → 识别问题 → 评估优先级 → 制定可执行的分阶段计划。

**核心输出**：`decision_plan.md`

**关键能力要求**：
- 能区分"日志声称已修复"和"代码实际仍存在问题"
- 能按安全 > 稳定性 > 性能 > 功能的优先级排序
- 能评估每个修复的风险和回退成本
- 能给出具体的代码改动方向（文件 + 行号 + 修复思路）

**Prompt 要点**：
```
1. 给出项目背景（一句话 + 技术栈）
2. 给出你已掌握的上下文（日志、代码审查发现等）
3. 要求输出：
   - 问题清单（严重程度 / 影响范围 / 是否本次修复）
   - 分阶段执行计划（Phase 1 必须修 / Phase 2 建议修 / Phase 3 未来增强）
   - 每个问题的具体修复方案（文件 + 行号 + 改动量 + 预计时间）
4. 要求写入指定文件路径
```

**经验教训**：
- 决策者可能产出误报（如我们遇到的 `setdefault` 拼写误判）。**协调者必须在开发者动工前验证决策者的发现**。
- 给决策者足够多的源码上下文（至少所有核心模块 + 入口文件），否则分析不完整。

---

### 2.2 角色二：开发者（Developer）

**职责**：按决策计划逐项实施代码修改。

**核心输出**：代码 diff / PR

**Prompt 要点**：
```
1. 引用决策计划文件路径
2. 列出需要修复的所有问题，每个问题给出：
   - 当前代码（old_string）
   - 修复方案描述
   - 预期的新代码逻辑
3. 明确约束：
   - 不要破坏 finally 块的安全保证
   - 保持代码风格一致
   - 不添加不必要的注释
   - 保持向后兼容
4. 要求逐一实施
```

**经验教训**：
- Agent 在后台运行（`run_in_background`）时可能没有写权限。可以让它**输出精确的 find/replace 对**，由协调者手动应用。
- 更优方案：让开发者在前台运行（非 background），这样它可以使用 Edit/Write 工具直接修改文件。
- 如果必须用 background，要求开发者输出 **old_string → new_string** 格式的修改清单。

---

### 2.3 角色三：测试者（Tester）

**职责**：设计测试用例 → 执行自动化验证 → 标记需人工验证的项。

**核心输出**：`test_report.md`

**测试用例应覆盖**：
| 类别 | 示例 |
|------|------|
| 正常流程 | 扫描、断网、恢复、测速 |
| 异常场景 | 无权限、设备离线、Ctrl+C、kill -9、多设备并发 |
| 代码级验证 | 静态分析关键逻辑路径 |
| Bug 回归 | 复现历史 Bug 确认修复仍有效 |

**Prompt 要点**：
```
1. 提供项目背景和全部关键源码路径
2. 要求设计四类测试：
   A. 正常流程测试
   B. 异常场景测试
   C. 代码级静态验证
   D. Bug 回归测试
3. 每个测试用例要求：目的 / 前置条件 / 操作步骤 / 预期结果
4. 提供可自动运行的 shell 测试脚本
5. 标记哪些测试需要 root/真实网络，列出完整操作步骤
6. 要求写入指定文件路径
```

**经验教训**：
- 测试者可以在开发者动工的同时开始工作（设计测试用例不需要等代码改完）。
- 但测试用例需要覆盖 **修复后的预期行为**，所以测试者需要先阅读决策计划。
- 让测试者提供可自动运行的脚本（语法检查、导入检查、逻辑断言），这些可以立即执行验证。

---

## 三、协作流程

### 3.1 标准工作流

```
Phase 0: 协调者准备上下文
  ├── 阅读开发日志、技术文档
  ├── 通读所有核心源码
  └── 汇总已知问题清单

Phase 1: 决策者分析（前台/后台均可）
  ├── 输入：项目上下文 + 源码 + 日志
  ├── 输出：decision_plan.md
  └── ⚠️ 协调者必须验证决策者的发现（避免误报）

Phase 2: 开发 + 测试并行（建议前台运行开发者）
  ├── 开发者：按计划实施代码修改
  │   └── 输出：代码 diff
  └── 测试者：编写测试计划
      └── 输出：test_report.md（含自动化脚本）

Phase 3: 协调者验证
  ├── 运行测试者的自动化脚本
  ├── 对比 diff 确认每项修改正确
  ├── 语法检查所有修改文件
  └── 确认 git 状态干净

Phase 4: 提交
  ├── git add <修改文件> + git commit
  └── 记录 commit hash 作为回退点
```

### 3.2 选择前台还是后台运行 Agent

| 模式 | 适用场景 | 风险 |
|------|----------|------|
| **前台** (默认) | 需要 Agent 直接写文件、改代码 | 等待时间较长，但结果可直接使用 |
| **后台** (`run_in_background: true`) | Agent 之间无依赖、纯调研、可并行 | 可能无写权限，需要协调者手动应用结果 |

**推荐**：开发者用前台模式（需要 Edit/Write 权限），决策者和测试者可用后台模式。

### 3.3 并行 vs 串行

```
决策者（串行，必须先完成）
    ↓
开发者 + 测试者（可并行）
    ↓
协调者审查 + 提交
```

- 决策者**必须**在开发者之前完成——开发者依赖计划。
- 测试者**可以**与开发者并行——测试用例设计不依赖代码改动（但需了解决策计划中的预期行为）。
- 真正的测试**执行**应在开发者完成后进行。

---

## 四、Prompt 模板

### 4.1 决策者 Prompt 模板

```
你是 [项目名] 的修复与升级决策者。
分析开发日志和代码，输出问题优先级清单和分阶段执行计划。

## 项目背景
[一句话描述 + 技术栈]

## 已掌握的信息
[日志摘要 / 已知 Bug 列表 / 代码审查发现]

## 你的任务
请输出一个文件 `[路径]/decision_plan.md`，包含：
1. **问题优先级排序**（安全 > 稳定性 > 性能 > 功能）
   - 列出所有当前仍存在的问题，按优先级从高到低
   - 每个问题标注：严重程度、影响范围、是否本次必须修复
2. **分阶段执行计划**
   - Phase 1（本次必须修复）
   - Phase 2（建议修复）
   - Phase 3（未来迭代）
3. **每个问题的具体修复方案**（涉及文件、关键改动点、风险）
```

### 4.2 开发者 Prompt 模板

```
你是 [项目名] 的开发工程师。
根据 `[路径]/decision_plan.md` 中的 Phase 1 计划，实施所有修复。

## 需要修复的问题
[逐条列出，包含：当前代码问题、修复方案、约束条件]

## 重要规则
- 修改前先 Read 文件
- 保持代码风格一致
- 不添加不必要的注释
- 不修改 finally 块的安全退出保证
- 所有改动必须保持向后兼容

请逐一实施修复。
```

### 4.3 测试者 Prompt 模板

```
你是 [项目名] 的测试工程师。
设计完整的测试计划并写入 `[路径]/test_report.md`。

## 你的任务

### 第一部分：测试计划设计
设计以下测试用例（每个包含：目的、前置条件、操作步骤、预期结果）：

#### A. 正常流程测试
[列出正常流程场景]

#### B. 异常场景测试
[列出异常场景]

#### C. 代码级验证（静态分析）
[列出需要静态分析的关键逻辑点]

#### D. Bug 回归测试
[列出需要回归验证的历史 Bug]

### 第二部分：可自动化的测试脚本
提供可以直接运行的 bash 脚本。

### 第三部分：标记「待用户验证」的测试
[列出需要特殊环境（root、真实网络）的测试]

请先读取关键源码再编写测试用例。
```

---

## 五、本次实践的得失复盘

### 做得好的

- **三角色分工清晰**：决策→实施→验证，各司其职
- **决策计划先行**：避免了边改边想的混乱
- **开发者输出精确 diff**：即使没有写权限，find/replace 对也能直接应用
- **测试者产出自动化脚本**：12 个可自动运行的测试提供了即时反馈
- **Git commit 粒度高**：代码修复和测试报告分开提交，回退灵活

### 可改进的

1. **决策者误报**：`setdefault` 拼写被误判为 Bug，实际代码正确。改进：协调者在决策者完成后、开发者动工前，必须对每个发现做独立验证（如 hex dump 确认）。

2. **开发者权限问题**：后台运行的 Agent 无法直接 Edit/Write。改进：开发者应使用前台模式，或要求输出 `old_string → new_string` 格式且需确保 old_string 完全匹配文件内容。

3. **测试者滞后**：测试报告在代码修改完成后才产出，无法做到 TDD。改进：测试者与开发者并行启动，测试者产出「应验证的点」清单后立即交给协调者。

4. **缺少 CI 检查**：没有自动运行 lint/type-check。改进：在测试报告中加入 `python3 -m py_compile` 和 `bash -n` 作为最低限度的自动化检查。

5. **Agent 间上下文传递**：每个 Agent 的 prompt 需要协调者手工组装项目上下文。改进：将项目概述、架构图、文件清单写入一个 `PROJECT_CONTEXT.md`，各 Agent prompt 直接引用。

---

## 六、快速启动清单

下次使用时，按以下步骤操作：

```
□ 1. 确保项目有 DEVELOPMENT_LOG 或类似文档记录历史
□ 2. 协调者通读核心源码（至少所有 .py 入口 + 核心模块）
□ 3. 启动决策者 Agent → 产出 decision_plan.md
□ 4. 协调者验证 decision_plan 中的每项发现（至少语法验证）
□ 5. 并行启动开发者（前台）和测试者（后台）
□ 6. 开发者完成后，运行测试者的自动化脚本
□ 7. 协调者审查 git diff，确认所有改动
□ 8. git commit（代码 + 文档分开提交）
□ 9. 如出现问题，git revert 回退
```

---

## 七、附录：本次角色 Prompt 完整版

### A. 决策者完整 Prompt

```
你是一个软件项目的修复与升级决策者。你的任务是分析 [项目名] 的开发日志和代码，
输出问题优先级清单和分阶段执行计划。

## 项目背景
[项目概述，一段话]

## 你已掌握的信息（开发日志中记录的问题修复状态）
[已知问题列表，区分"日志声称已修复"和"代码审查确认为仍存在"]

### 确认的代码缺陷：
[逐条列出，包含文件:行号、当前代码、问题描述、影响]

### 未来改进方向：
[从开发日志中提取的 future work 列表]

## 你的任务
请输出一个文件 `[路径]/decision_plan.md`，包含：
1. 问题优先级排序（安全 > 稳定性 > 性能 > 功能）
2. 分阶段执行计划（Phase 1 必须修 / Phase 2 建议修 / Phase 3 未来增强）
3. 每个问题的具体修复方案（技术方案概述）
4. 风险矩阵

请确保计划具体、可执行，包含修复涉及的文件和关键改动点。
```

### B. 开发者完整 Prompt

```
你是 [项目名] 项目的开发工程师。
请根据 `[路径]/decision_plan.md` 中的 Phase 1 计划，实施所有 N 项修复。

## 项目背景
[一句话 + 技术栈 + 项目路径]

## 需要修复的问题
[逐条列出，格式：
### P1-X: 文件 — 问题描述
**当前代码**（片段）
**需求**（逐条列出改动要求）
]

## 重要规则
- 修改前先 Read 文件
- 保持代码风格一致
- 不添加不必要的注释
- 不修改 finally 块的安全退出保证
- 所有改动必须保持向后兼容

请逐一实施这 N 项修复。
```

### C. 测试者完整 Prompt

```
你是 [项目名] 项目的测试工程师。
请设计完整的测试计划并写入 `[路径]/test_report.md`。

## 项目背景
[一句话 + 技术栈 + 项目路径]

## 你的任务

### 第一部分：测试计划设计
设计以下测试用例（每个包含：测试目的、前置条件、操作步骤、预期结果）：

#### A. 正常流程测试 N 项
[列出场景]

#### B. 异常场景测试 N 项
[列出场景]

#### C. 代码级验证
[列出需要静态分析的关键代码路径]

#### D. Bug 回归测试
[引用开发日志中的历史 Bug 编号]

### 第二部分：可复现操作步骤
对每个测试用例提供精确命令或 GUI 操作步骤。

### 第三部分：自动化测试脚本
提供可直接运行的 bash 脚本。

### 第四部分：待用户验证清单
标记需要 root/真实网络环境的测试项。

请先读取项目关键源码再编写测试用例。
```
